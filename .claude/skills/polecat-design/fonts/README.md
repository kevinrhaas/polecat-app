The Hanken Grotesk binaries are not vendored twice in this repo: the app ships
them as woff2 in `vendor/polecat-shell/fonts/` (with `fonts.css` alongside).
The full 18-file TTF set lives in the canonical skill copy at
kevinrhaas/polecat-platform → .claude/skills/polecat-design/fonts/.
